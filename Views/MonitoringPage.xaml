<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Desktop.Views.MonitoringPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Desktop.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <NavigationView x:Name="RootNavigation"
                    PaneDisplayMode="Left"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False"
                    IsPaneToggleButtonVisible="True"
                    OpenPaneLength="248"
                    CompactPaneLength="52"
                    SelectionChanged="RootNavigation_SelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="概览" Tag="Overview">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Home" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="配置" Tag="Connection">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Globe" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="采集策略" Tag="Strategy">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Edit" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="更新与日志" Tag="Updates">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Download" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <InfoBar Grid.Row="0"
                         Margin="24,18,24,0"
                         IsOpen="{Binding IsErrorVisible}"
                         Severity="Error"
                         Message="{Binding ErrorMessage}" />
                <InfoBar Grid.Row="1"
                         Margin="24,10,24,0"
                         IsOpen="{Binding IsNoticeVisible}"
                         Severity="Informational"
                         Message="{Binding NoticeMessage}">
                    <InfoBar.ActionButton>
                        <controls:UpdateInfoBarActions
                            Visibility="{Binding AreUpdateActionsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                            DismissRequested="UpdateActions_DismissRequested"
                            SkipRequested="UpdateActions_SkipRequested" />
                    </InfoBar.ActionButton>
                </InfoBar>
                <Frame x:Name="ContentFrame" Grid.Row="2" IsNavigationStackEnabled="False">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromHorizontalOffset="28"
                                                      FromVerticalOffset="0"
                                                      IsStaggeringEnabled="False" />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </Grid>
        </NavigationView.Content>
    </NavigationView>
</Page>
